name: transport
description: Object is moved horizontally to a new location with gripper closed.

detection:
  strategy: sustained_horizontal_motion
  preconditions:
    gripper_state: closed    # gripper[t] < 0.5
  parameters:
    xy_velocity_threshold: 0.005
    min_duration_frames: 3
  state_requirements:
    velocity: true           # uses xy plane (first 2 dims)
    gripper: true
  confidence: 0.85

constraints:
  # Typically occurs between lift and place
  temporal_order: [grasp, lift]
  gripper_must_remain: closed

natural_language:
  templates:
    - "transport the object to the destination"
    - "move {object} to {location}"
    - "carry {object} across"
